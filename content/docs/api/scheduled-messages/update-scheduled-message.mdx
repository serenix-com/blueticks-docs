---
title: Update scheduled message
description: >-
  Edit a previously-queued message that has not dispatched yet (status
  `pending`).
full: true
_openapi:
  method: PATCH
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Edit a previously-queued message that has not dispatched yet (status
          `pending`).


          Accepts the **same flat fields as [Schedule
          message](#schedule-message)** — `type`, `text`, `mediaUrl`,
          `mediaBase64`, `mediaKind`, `mediaFilename`, `pollQuestion`,
          `pollOptions`, `pollAllowMultiple`, `to`, `sendAt`, `replyTo`,
          `secret` — but **every field is optional**. Send only the fields you
          want to change; at least one is required.


          Notes:

          - A new `mediaUrl` / `mediaBase64` is re-hosted to GCS just like on
          create; `mediaKind` / `mediaFilename` can be edited on their own.

          - Editing `text` re-derives the link-preview card (auto-attached when
          the text contains a URL).

          - `sendAt` reschedules (must be ≥10s in the future, ≤365 days).

          - Returns 400 once the message has advanced past the editable window
          (status not `pending`).
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Accepts the **same flat fields as [Schedule message](#schedule-message)** — `type`, `text`, `mediaUrl`, `mediaBase64`, `mediaKind`, `mediaFilename`, `pollQuestion`, `pollOptions`, `pollAllowMultiple`, `to`, `sendAt`, `replyTo`, `secret` — but **every field is optional**. Send only the fields you want to change; at least one is required.

Notes:
- A new `mediaUrl` / `mediaBase64` is re-hosted to GCS just like on create; `mediaKind` / `mediaFilename` can be edited on their own.
- Editing `text` re-derives the link-preview card (auto-attached when the text contains a URL).
- `sendAt` reschedules (must be ≥10s in the future, ≤365 days).
- Returns 400 once the message has advanced past the editable window (status not `pending`).

<APIPage document={"./openapi.json"} operations={[{"path":"/v1/scheduled-messages/{id}","method":"patch"}]} />