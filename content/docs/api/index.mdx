---
title: API Reference
description: Complete reference for the Blueticks public API v1.
---

import { Cards, Card } from 'fumadocs-ui/components/card';
import { Download, Link as LinkIcon, FileJson } from 'lucide-react';

export const showStaging = !process.env.NEXT_PUBLIC_API_SERVERS || process.env.NEXT_PUBLIC_API_SERVERS.includes('stg-');

This section documents every endpoint in the Blueticks public API v1.
Endpoints are grouped by resource.

All endpoints require an API key passed as `Authorization: Bearer <key>`.
See the [Authentication](/docs/authentication) guide for how to obtain and
configure a key.

## Base URL

```
https://api.blueticks.co
```

## Use in Postman

A Postman collection + two environment files are regenerated on every spec
sync — they track the live API without manual updates.

**Flow:** import the collection → import one environment (Production or
Staging) → paste your API key into the env's `apiKey` field → start hitting
endpoints. Run "🔑 Authentication / Validate API key" first to confirm.

<Cards>
  <Card
    icon={<Download />}
    title="Postman collection"
    description="Every endpoint, organized by tag. Includes a Validate-API-key request to confirm auth works."
    href="/blueticks.postman_collection.json"
    external
  />
  <Card
    icon={<LinkIcon />}
    title="Production environment"
    description="baseUrl pre-set to api.blueticks.co. Paste your API key into apiKey once."
    href="/blueticks.production.postman_environment.json"
    external
  />
  {showStaging && (
    <Card
      icon={<LinkIcon />}
      title="Staging environment"
      description="baseUrl pre-set to stg-api.blueticks.co for safe experimentation."
      href="/blueticks.staging.postman_environment.json"
      external
    />
  )}
  <Card
    icon={<FileJson />}
    title="Raw OpenAPI spec"
    description="Prefer to import the spec yourself? Works with any OpenAPI-aware tool."
    href="/openapi.json"
    external
  />
</Cards>

## Response format

Successful responses are JSON objects with resource-specific shapes. Errors
use the envelope described in [Errors & retries](/docs/errors).

## Try it in the browser

Each operation page below embeds an interactive playground. Paste an API
key into the auth field and send real requests against
`https://api.blueticks.co`.
